// import {setAllData} from "@/firebase/firebase";
//
//
// onmessage = function (event) {
//     event.waitUntil( setAllData(event.data.data, event.data.userId, event.data.boardId).finally(()=>{
//         close()
//     }))
//
// };